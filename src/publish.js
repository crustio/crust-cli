module.exports = {
    default: (cid) => {
        // TODO: call ipfs get file size, and place storage order with (cid, size, seeds)
        console.log(`Try to publish to Crust Network with ${cid}`);
    }
}